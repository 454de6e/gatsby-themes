// Theme option `preset` sets the base theme as @maiertech/preset.
// Customizations are merged with the base theme.
const customizations = {
  styles: {
    pre: {
      p: 2,
    },
  },
};

export default customizations;
